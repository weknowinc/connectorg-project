<?php

/**
 * @file
 * Contains owp_tool.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function owp_tool_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the owp_tool module.
    case 'help.page.owp_tool':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Module to handle all information from tools') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function owp_tool_theme() {
  return [
    'owp_tool' => [
      'render element' => 'children',
    ],
  ];
}
